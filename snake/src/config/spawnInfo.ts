export default Object.freeze({
    initalMatch: Math.floor(Math.random() * (5 - 1) + 1),
    snakeStart: (`${Math.floor(Math.random() * (19 - 2) + 2)}, ${Math.floor(Math.random() * (19 - 2) + 2)}`),
})